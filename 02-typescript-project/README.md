Initialize folder

`npm init -y`

Install typescript for development

`npm i typescript --save-dev`

Add to package.json

```
"scripts": {
    "tsc": "tsc"
  },
```

Create tsconfig file

`npm run tsc -- --init`

Compile project

`yarn run tsc`
