import { UcbHighlightDirective } from './ucb-highlight.directive';

describe('UcbHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new UcbHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
